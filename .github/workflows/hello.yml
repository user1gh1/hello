name: CI

on:
  schedule:
#time UTC
    - cron: '20 13 * * *'
jobs:
  notifier:
    runs-on: ubuntu-latest
    steps:
      - name  : Notification_success
        run   : curl -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage -d chat_id=${{ secrets.TELEGRAM_TO }} -d text='qwerty'
      
